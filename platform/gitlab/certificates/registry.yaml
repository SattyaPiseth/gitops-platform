apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-registry-tls
  namespace: gitlab
spec:
  secretName: gitlab-registry-tls

  issuerRef:
    name: k8s-internal-ca
    kind: ClusterIssuer

  dnsNames:
    - registry.k8s.tss.local
