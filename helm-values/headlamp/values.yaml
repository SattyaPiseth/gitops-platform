config:
  pluginsDir: /headlamp/plugins

ingress:
  enabled: false

replicaCount: 3

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

service:
  port: 80
  type: ClusterIP

clusterRoleBinding:
  create: false